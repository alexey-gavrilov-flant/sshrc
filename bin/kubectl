cmd=$(find.bin kubectl)
if [ "$cmd" != "" ]; then
  if [ "$EUID" -ne 0 ]; then
    exec sudo $cmd --kubeconfig=/root/.kube/config "$@"
  else
    exec $cmd --kubeconfig=/root/.kube/config "$@"
  fi
fi
