kubectl=kubectl
if [ -f /opt/deckhouse/bin/kubectl ]; then kubectl=/opt/deckhouse/bin/kubectl; fi
if [ "$EUID" -ne 0 ]; then
  exec sudo $kubectl --kubeconfig=/root/.kube/config "$@"
else
  exec $kubectl --kubeconfig=/root/.kube/config "$@"
fi
