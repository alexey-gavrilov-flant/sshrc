#!/bin/bash
for pod in $(k.find.deckhouse); do
  kubectl -n d8-system get pod "${pod}" -owide --no-headers=true
  kubectl -n d8-system exec -it "${pod}" -c deckhouse -- deckhouse-controller queue list "$@"
done
